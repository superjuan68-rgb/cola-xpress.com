# Baseline Audit — Followup (2026-05-12)

References: `2026-05-12-baseline.md`

---

## Open questions: ANSWERED

| Question | Answer |
|---|---|
| Canonical: www or non-www? | **Non-www** (`https://cola-xpress.com/`). The strategy doc had a typo. Site code is correct — no mass canonical rewrite needed. |
| Social handles? | Facebook only for now: `https://www.facebook.com/ColaXpress` |
| Monetization model? | Vivosun affiliate + YouTube ads + sponsorship |
| Priority keywords? | Not pre-decided — see `2026-05-12-keyword-priority-list.md` for a derived top-20 |
| Old domain status? | `colaxpress.cannahustle.nyc` **still resolves** — needs the migration .htaccess deployed |

---

## What's been implemented

### Branch: `fix/canonical-org-schema` (off `main`, ready to review/merge)
**URL:** https://github.com/superjuan68-rgb/cola-xpress.com/tree/fix/canonical-org-schema

| Change | Files | Effect |
|---|---:|---|
| Section canonicals normalized to directory form (`/grow/index.html` → `/grow/`) | 5 | Eliminates duplicate-content split with the directory URL Apache also serves |
| Absolute URLs normalized in canonical/og:url/schema url/breadcrumb item | 80 swaps across 50 HTML files | Consistency across all schema and metadata |
| Internal `href="*/index.html"` rewritten to clean directory paths | 957 links across 50 HTML files | Removes 957 redirect hops from internal crawl |
| `.htaccess` rule added to 301 any remaining `/*/index.html` → `/*/` | 1 | Catches external links still using the old form |
| Sitemap entries normalized | 5 | Sitemap now matches canonicals |
| `rebuild_sitemap.py` patched | 1 | Next regeneration won't reintroduce the bad form |
| `inject_imageobject.py` patched | 1 | Next injection won't generate bad URLs |
| Organization JSON-LD added to homepage with Facebook sameAs | 1 | Knowledge-graph eligibility; powers the Google Business panel |

**To merge:**
```bash
# Local review
git fetch origin
git checkout fix/canonical-org-schema
# inspect changes
git diff main..HEAD --stat

# Merge when satisfied
git checkout main
git merge --no-ff fix/canonical-org-schema
git push origin main
```
Then **regenerate sitemap** to confirm no regression:
```bash
python rebuild_sitemap.py
```
Sitemap should report 51 URLs and `Coverage: 100% — all images indexed.` If anything else, stop and ping me.

---

## What still needs to happen (not done yet, by category)

### Migration (legacy domain still resolves)
Since `colaxpress.cannahustle.nyc` is still live, deploy `migration/apache/OLD-DOMAIN.htaccess` on **that server's** `public_html/.htaccess`. The file is already in this repo on the `claude/site-audit-migration-IKDNa` branch. After deploy, verify:
```bash
curl -I https://colaxpress.cannahustle.nyc/
# Expect: HTTP/2 301
# Expect: Location: https://cola-xpress.com/

curl -I https://colaxpress.cannahustle.nyc/grow/
# Expect: HTTP/2 301
# Expect: Location: https://cola-xpress.com/grow/
```
Then go to GSC Settings → Change of Address. Full runbook in `migration/google-search-console-checklist.md`.

### Trust scaffolding (from baseline audit, item #5 — `severity: medium-high`)
Not yet implemented. These need real assets from you to do well:
- Author bylines on top 15 guides (need: real name + bio)
- Founder photo + paragraph on `/guides/about.html`
- Newsletter signup form on guide pages (need: which provider — ConvertKit, Beehiiv, Buttondown?)
- Footer column linking Facebook (already in Organization schema, now add it visually)

### Affiliate disclosure (new requirement from your answer)
You confirmed Vivosun affiliate. FTC requires conspicuous affiliate disclosure on every page that includes affiliate links. Suggested text, to add at the top of any page that links to Vivosun (specifically `/equipment/vivosun-vgrow-dwc-guide.html` and `/equipment/what-to-buy-first.html`):

```html
<aside class="disclosure">
  <strong>Affiliate disclosure:</strong> ColaXpress earns a commission when readers
  purchase Vivosun products through links on this page. The recommendation reflects
  hands-on use, not the commission. You pay the same price either way.
</aside>
```

Also add a permanent `/guides/affiliate-disclosure.html` page and link it from the footer.

### YouTube channel + VideoObject
Once Episode 1 is published on YouTube:
1. Add `contentUrl` and `embedUrl` to the VideoObject schema in `/watch/episode-01.html`
2. Add the YouTube channel URL to the homepage Organization `sameAs` array (currently only Facebook)
3. Consider embedding the YouTube player on `/watch/episode-01.html` (video thumbnail + embed = higher engagement signal)

### Keyword targeting
See `2026-05-12-keyword-priority-list.md` — top 20 derived from existing content corpus. Tier 1 (commercial, Vivosun affiliate) is what to push first.

---

## Sandbox limitations to be aware of

- **Cannot reach the live sites from here.** `curl` and WebFetch both return `403 host_not_allowed` for `cola-xpress.com` and `colaxpress.cannahustle.nyc`. All findings come from direct inspection of files in `public_html/` on `main`.
- **No GSC access.** When you have GSC exports (Performance, Coverage, Sitemaps reports), drop the CSVs in `audits/gsc-exports/<date>/` and I can use them on the next pass.
- **No competitor SERP data.** To run Agent 06 (competitor intel), I need either the top 5 ranking URLs per priority keyword, or an Ahrefs/Semrush export.

---

## Commit note

The fix branch commit (`f83073c`) is **unsigned**. The local commit-signing service was returning HTTP 400 at commit time. User explicitly approved the one-off bypass for this commit. All earlier commits in this session were signed. If the signing service stays down, future commits will need the same explicit approval.
